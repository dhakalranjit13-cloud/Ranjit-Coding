#include<stdio.h>
int name(int a,int b,int c);
void main()
{
    int a,b,c,d;
    printf("Enter a & b & c:");
    scanf("%d%d%d",&a,&b,&c);
    d=name(a,b,c);
    printf("Average %d",d);
    
}
int name(int a,int b,int c)
{
    int d;
    d=(a+b+c)/3;
    return d;
}
